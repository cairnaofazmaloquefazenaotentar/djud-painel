/**
 * @deprecated Use `components/dashboard/metric-card` instead.
 * Este arquivo existe apenas para compatibilidade com imports legados.
 */
export { MetricCard } from "@/components/dashboard/metric-card";
